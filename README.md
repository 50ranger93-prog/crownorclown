Data only. The Go get it leaderboard lives in board.json on this branch.

It is written by /api/board through the GitHub Contents API, one commit per score
that actually makes the top ten. Deployments are off here (vercel.json above, and
main disables this branch too) so posting a score never triggers a build.

Nothing here is source. Do not merge this branch into main.
